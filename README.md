# aiAmy
```
python -m venv .venv #Create a virtual env (a copy of your python install basically) so the libs get installed only inside this ".venv" folder and not everywhere in the system.
.venv/Scripts/activate #Activate the venv.
pip install -e . #Read the pypriject.toml and install the libs.
python aiAmy/main.py #Launch the program.
```